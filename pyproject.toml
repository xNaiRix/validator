[project]
name = "validators"
version = "0.1.0"
description = "A simple validator library"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["validators*"]
exclude = ["myenv*", "tests*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"